# Pin kubelet node addressing to the physical LAN on every cluster node.
# Apply this patch to the control plane and both workers so a host-networked
# Tailscale pod cannot cause kubelet to select a 100.64.0.0/10 address.
machine:
  kubelet:
    nodeIP:
      validSubnets:
        - 192.168.1.0/24
